machine:
  node:
    version: 10.11.0
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
deployment:
  production:
    branch: master
    heroku:
      appname: dankbot-69
test:
  override:
    - yarn test
  pre:
    - yarn run lint
